<nav class="navbar navbar-expand-lg navbar-light bg-light mb-5">
    <a class="navbar-brand" href="#">LOGO Blogging</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="#">Tendances</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('users_list') }}">Utilisateurs</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">About</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                {% if app.user %}
                    {% set notificationLength = NotificationService.getNotification['Length'] %}
                    <div class="d-flex justify-content-start">

                        {% include '_partials/navbar/notification.html.twig' %}

                        <div class="mr-5">
                            <a
                                    class="nav-link"
                                    href="#"
                                    id="navbarDropdownMenuLink"
                                    role="button"
                                    data-toggle="dropdown"
                                    aria-expanded="false"
                            >
                                <i class="fas fa-user"></i>
                            </a>
                            <div class="dropdown-menu dropdown-menu-right">
                                <a href="{{ path('user_id', {'user': app.user.username}) }}" class="dropdown-item">Profil</a>
                                <button class="dropdown-item" type="button">Another action</button>
                                <a href="{{ path('authentication_logout') }}" class="dropdown-item">Se déconnecter</a>
                            </div>
                        </div>
                    </div>

                {% else %}
                    <a class="nav-link" href="{{ path('authentication_account') }}">Connexion / Inscription</a>
                {% endif %}



            </li>
        </ul>
    </div>
</nav>